Personal Sample Code - Zuri Random Questions Solved using NodeJS

Note: There is an issue in the last index of the JSON file - isMarried & isDivorced are said to be 'true'.
However, the solution was carried out like that

Questions
// map true the user and get the following
// a. create a new array called usersAge and store the age of users in it
// b, create suitable variable and store the following
  // divorced users
  // married users
  // male users
  // female users
//  create a variable to store users sorted by decrease in age


// a. create a new ARRAY from the array of users above and store names of users
// b.create a new ARRAY from the array of users above and store gender of users
// c. create a new array that conatins a, and b above